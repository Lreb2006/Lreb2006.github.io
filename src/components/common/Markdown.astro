---
import "@/styles/markdown.css";
import "@/styles/markdown-extend.styl";

interface Props {
	class: string;
}
const className = Astro.props.class;
---
<div data-pagefind-body class={`prose prose-sm md:prose-lg dark:prose-invert max-w-none! custom-md ${className}`}>
    <slot/>
</div>
